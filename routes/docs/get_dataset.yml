get:
  summary: Get a dataset by ID
  description: Retrieve a dataset using its unique ID
  parameters:
    - in: path
      name: dataset_id
      required: true
      schema:
        type: string
  responses:
    200:
      description: Dataset retrieved
    404:
      description: Dataset not found
